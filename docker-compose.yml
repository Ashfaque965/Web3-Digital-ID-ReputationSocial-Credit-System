version: '3.8'

services:
  hardhat:
    build: .
    ports:
      - "8545:8545"
    volumes:
      - .:/app
    command: npx hardhat node

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_REPUTATION_CONTRACT=0x...
    depends_on:
      - hardhat